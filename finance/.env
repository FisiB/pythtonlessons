# Example .env (copy to .env and set real secret in your local machine)
# Do NOT commit a .env with real secrets.

# Secret used to sign JWTs (replace with a strong random string)
JWT_SECRET=replace_with_a_long_random_value

# What algorithm to use to sign JWTs
JWT_ALGO=HS256

# API host/port used by the Streamlit client (used as default if not specified elsewhere)
API_HOST=http://127.0.0.1:8000

# Local DB path (relative to the finance folder)
DATABASE_PATH=./data/finance.db